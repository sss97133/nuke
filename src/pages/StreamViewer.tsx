
import React from 'react';
import { StreamViewer as StreamViewerComponent } from '@/components/streaming/viewer/StreamViewer';

const StreamViewer: React.FC = () => {
  return <StreamViewerComponent />;
};

export default StreamViewer;
