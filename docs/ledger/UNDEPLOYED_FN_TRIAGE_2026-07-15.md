# Undeployed-fn frontend calls — full triage (2026-07-15)

> Produced by NUKE-MAINFRAME post-crash sync. 43 frontend→edge-function calls that 404 in prod, each classified with evidence via a fan-out investigation (workflow wf_8d054047-196). This is the executable queue: exact target + edit per function. **Verdicts are hypotheses vetted against the ledger — re-verify against runtime before executing each (liveness law rule 3).**

Class counts: 6 deploy, 11 repoint, 5 retired-feature-guard, 18 guard, 3 already-fine.

## ⚠ SAFETY FLAGS (read before touching the deploy class)

- **auto-fix-vehicle-profile — DO NOT DEPLOY.** It does blind service-role UPDATEs to `vehicles` identity fields (year/make/model/VIN) from AI vision output, bypassing ingest-observation AND overwriting non-empty values — a direct trust-invariant violation (the 1974-Blazer auto-promotion class). Guard the call site or route the write through ingest-observation first. Currently inert (undeployed/404-caught) — leaving it undeployed is the safe state.
- **auto-merge-duplicate-orgs — OWNER CALL.** CAPABILITY_MAP says 'DEPLOY IT FIRST', but it auto-merges orgs at confidence≥0.75 on crude char-level similarity with no human gate — a foot-gun on Skylar's own org data. Not deployed pending his ruling.
- **report-marketplace-sale — BLOCKED on schema.** Requires table `marketplace_sale_reports`, which does NOT exist in prod (verified 2026-07-15). Creating it is an owner-only schema change. Guard the call or create the table first.
- **extract-bat-parts-brands — SOURCE BUG.** `index.ts:40` destructures a `linkToImageTags` boolean that shadows the helper fn of the same name at :265 → TypeError on every extraction. Fix the shadow before deploying.
- **analyze-vehicle-documents — deployable but FE contract mismatch survives.** `VehicleLedgerDocumentsCard.tsx` sends `{vehicleId,limit}` but the fn needs `vehicle_id` and returns a different response shape — fix the caller in the same PR or the button stays broken post-deploy.

- **admin-update-vehicle-field — ✅ DEPLOYED 2026-07-15.** Clean: human-admin gated (admin_users.is_active), field whitelist + ownership-field blocklist, no testimony writes. Both callsites already error honestly; no FE change. Smoke-tested (401 unauth, was 404).

## DEPLOY (source in repo, no canonical competitor)

| fn | target | call sites | action |
|----|--------|-----------|--------|
| `admin-update-vehicle-field` | — | VehicleDataGapsCard.tsx:304; ValidationPopupV2.tsx:558 | Deploy, no frontend edits needed (both callsites already error honestly and match the fn's {ok, error} response shape). Steps: (1) supabase functions deploy admin-update-vehicle-field --project-ref <ref from docs/ledger/facts.json>. Secrets are Supabase-runtime-provided (SUPABASE_URL/ANON/SERVICE_ROLE), nothing custom to set. (2) Verify tables exist: select 1 from admin_users/vehicle_field_sources/timeline_events lim |
| `analyze-vehicle-documents` | analyze-vehicle-documents | VehicleLedgerDocumentsCard.tsx:167; VehicleDocumentIntelligence.tsx:92 | 1) Deploy: `supabase functions deploy analyze-vehicle-documents --no-verify-jwt`. Verify first: `supabase secrets list` shows ANTHROPIC_API_KEY (SUPABASE_URL/SERVICE_ROLE_KEY are platform-injected); confirm tables exist in prod (`select 1 from service_records limit 1; select 1 from timeline_events limit 1; select 1 from vehicle_documents limit 1`). Smoke test: `curl -X POST $SUPA/functions/v1/analyze-vehicle-document |
| `auto-fix-vehicle-profile` | — | BulkImageUploader.tsx:253 | Deploy: (1) `supabase functions deploy auto-fix-vehicle-profile`; (2) verify secrets — ANTHROPIC_API_KEY must be set in edge-function secrets (SUPABASE_URL/SERVICE_ROLE_KEY are injected automatically): `supabase secrets list \| grep ANTHROPIC`; (3) confirm vehicles + vehicle_images tables exist (they are core live substrate per ledger); (4) smoke test: `curl -X POST $VITE_SUPABASE_URL/functions/v1/auto-fix-vehicle-pr |
| `auto-merge-duplicate-orgs` | — | CreateOrganization.tsx:96 | Deploy, don't touch the callsite: (1) `supabase functions deploy auto-merge-duplicate-orgs --no-verify-jwt` (secrets SUPABASE_URL/SERVICE_ROLE_KEY are platform-injected, nothing to set). (2) Verify tables exist: businesses, organization_vehicles, organization_images, organization_contributors, organization_followers, business_timeline_events (all already used by the live CreateOrganization flow). (3) Verify downstrea |
| `extract-bat-parts-brands` | extract-bat-parts-brands | BATListingManager.tsx:119 | Deploy path, but FIX TWO SOURCE BUGS FIRST: (1) supabase/functions/extract-bat-parts-brands/index.ts:40 destructures `linkToImageTags` (boolean) which shadows the helper function of the same name at :265 — line 147 `await linkToImageTags(stored, vehicleId, supabase)` calls the boolean and throws TypeError on every extraction with linking enabled; rename the request flag (e.g. `shouldLinkToImageTags`). (2) :70 uses ra |
| `report-marketplace-sale` | report-marketplace-sale | FacebookMarketplaceImport.tsx:89 | Deploy, don't touch the callsite: (1) verify tables in prod: SELECT from marketplace_listings, marketplace_sale_reports limit 1; SELECT proname FROM pg_proc WHERE proname IN ('process_marketplace_sale_reports','award_discovery_points'). (2) supabase functions deploy report-marketplace-sale --no-verify-jwt (via supabase-deploy.yml per production-engineering rule, not hand-applied if the workflow covers it). (3) Smoke  |

## REPOINT (a deployed canonical covers it)

| fn | target | call sites | action |
|----|--------|-----------|--------|
| `ai-agent-supervisor` | analyze-with-claude | VehicleExpertChat.tsx:824 | In VehicleExpertChat.tsx, replace the whole two-step invoke chain (lines 804-831): delete the vehicle-expert-agent primary and the ai-agent-supervisor .catch fallback, and call analyze-with-claude directly: supabase.functions.invoke('analyze-with-claude', { body: { vehicle_id: vehicleId, prompt: currentInput, mode: 'slow' } }) — optionally prepend the last ~10 conversation turns into the prompt string, since analyze- |
| `create-setup-session` | setup-payment-method | ProfileVerification.tsx:391 | In ProfileVerification.tsx startAddCard() (lines 386-407): the canonical fn has a DIFFERENT contract — create-setup-session is a redirect-based Stripe Checkout setup session ({success_url, cancel_url} → {ok, url}), while setup-payment-method expects a client-collected {payment_method_id} (Stripe.js Elements/CardElement flow, as auctionPaymentService.ts setupPaymentMethod already does) and attaches+defaults it on the  |
| `data-quality-workforce` | direct invocation of the deployed strategy fns it dispatched: batch-vin-decode,  | DataQualityDashboard.tsx:188 | In DataQualityDashboard.tsx triggerWorker (lines 184-201): (1) add a STRATEGY_FN map {batch-vin-decode→'batch-vin-decode', batch-ymm-propagate→'batch-ymm-propagate', enrich-factory-specs→'enrich-factory-specs', enrich-vehicle-profile-ai→'enrich-vehicle-profile-ai', compute-vehicle-valuation→'compute-vehicle-valuation'} and invoke the mapped fn directly with body {} (or the fn's own batch params — read each fn's index |
| `dealer-ai-assistant` | analyze-with-claude | DealerAIAssistant.tsx:132 | In DealerAIAssistant.tsx line 132: replace invoke('dealer-ai-assistant', {body:{organizationId, message, attachments, conversationHistory}}) with invoke('analyze-with-claude', {body:{prompt: <input + serialized last-10 conversationHistory>, ...}}). Response-shape notes: analyze-with-claude requires `prompt` (string, 400 'prompt required' otherwise) and accepts vehicle_id/mode — it does NOT accept organizationId, atta |
| `extract-and-route-data` | extract-vehicle-data-ai | aiDataIngestion.ts:498; aiDataIngestion.ts:581; aiGateway.ts:157 | 1) aiDataIngestion.ts:498 (text): repoint invoke to 'extract-vehicle-data-ai' with body { textContent: text, source: 'user_text_input', save_to_db: false }. Response shape differs from the old contract: extract-vehicle-data-ai returns extracted/normalized vehicle fields + confidence, NOT {success, vehicleData, receiptData, provider, model} — adapt the mapping: vehicleData ← the returned normalized vehicle object, rec |
| `generate-auction-description` | generate-vehicle-description | CreateAuctionListing.tsx:94 | In CreateAuctionListing.tsx generateAIDescription(): (1) delete the client-side vehicles+timeline_events pre-fetch (lines ~76-91) — the canonical fn fetches its own context server-side; (2) change the invoke to supabase.functions.invoke('generate-vehicle-description', { body: { vehicle_id: selectedVehicleId } }); (3) response shape already matches — keep data?.description handling as-is. Note the fn also persists the |
| `image-ai-chat` | analyze-with-claude | ImageAIChat.tsx:79 | Do NOT deploy image-ai-chat (graveyard per CAPABILITY_MAP.md:36; its write path also violates trust invariants — see risk). Edit ImageAIChat.tsx:79: repoint supabase.functions.invoke('image-ai-chat', ...) to 'analyze-with-claude', adapting the request body to that function's contract (it is the BYOK funnel — verify supabase/functions/analyze-with-claude/index.ts for its expected fields; the image question can be pass |
| `match-vehicles-by-images` | check-image-vehicle-match | vehicleImageMatcher.ts:31; AIDataIngestionSearch.tsx:948; AIDataIngestionSearch.tsx:1099 | Edit only vehicleImageMatcher.ts (both AIDataIngestionSearch call sites then work unchanged). NOT a drop-in swap — shapes differ: check-image-vehicle-match takes {mode:'check'\|'build_signatures'\|'disambiguate', vehicle_id, user_id, image_ids?, dry_run?} and returns per-image MatchResult[] {status: confirmed\|mismatch\|ambiguous\|unrelated\|reassigned, confidence, suggested_vehicle_id, reason}; it validates already- |
| `scrape-bat-member` | extract-bat-profile-vehicles | BaTBulkImporter.tsx:34 | Repoint the one call in BaTBulkImporter.tsx extractListingsFromMemberPage (line 34). The canonical fn's input differs (it takes {profile_url}, not {memberUrl}) and its output differs (it returns {listing_urls: string[], listings_found} at index.ts:285-286, not {listings}). Both are already satisfiable without touching the fn. Concrete change:\n\n  const { data, error } = await supabase.functions.invoke('extract-bat-p |
| `validate-bat-image` | validate-vehicle-image | VehicleComments.tsx:725 | Repoint the single callsite at VehicleComments.tsx:725 from 'validate-bat-image' to 'validate-vehicle-image', adapting BOTH request and response shapes (they differ — a bare rename will silently 400/misread):  REQUEST — caller currently sends { image_id, image_url, vehicle_id, expected_vehicle:{year,make,model} }. Canonical (ValidationRequest, index.ts:13-20) requires `image_url` and accepts `expected_vehicle` as a S |
| `vehicle-expert-agent` | compute-vehicle-valuation + analyze-with-claude | SmartInvoiceUploader.tsx:315; VehicleExpertChat.tsx:806 | Repoint each call site to its deployed canonical owner; do NOT redeploy vehicle-expert-agent (disposition = SUPERSEDED, and CANONICAL_LEDGER names other owners).  SmartInvoiceUploader.tsx:315 — Replace invoke('vehicle-expert-agent',{body:{vehicleId}}) with invoke('compute-vehicle-valuation',{body:{vehicleId}}). Map its response to the existing chip: read the canonical fn's estimate field (confirm exact key — likely e |

## RETIRED-FEATURE GUARD (owner-only removal — do NOT delete UI)

| fn | target | call sites | action |
|----|--------|-----------|--------|
| `create-payout` | — | cashBalanceService.ts:114 | Guard, don't deploy, don't repoint (no canonical payout function exists and the capability is retired). Concrete edit in nuke_frontend/src/services/cashBalanceService.ts withdrawCash(): replace the supabase.functions.invoke('create-payout', ...) block (lines 114-121) with an immediate honest failure — `throw new Error('Withdrawals are not available — this feature has been retired.')` — placed BEFORE the balance check |
| `create-shipping-listing` | null — no canonical replacement; CAPABILITY_MAP.md:76 rules the entire 'Vehicle  | vehicleTransactionService.ts:143; shippingService.ts:47 | Guard both call sites with the repo's existing ghost-ref pattern (same style as the TODO(ghost-ref 2026-07-12) guards already in these two files). (1) vehicleTransactionService.ts:141-152 — replace the invoke block with: `// TODO(ghost-ref): create-shipping-listing RETIRED (CAPABILITY_MAP.md 'Vehicle shipping' — CAPABILITY RETIRED; disposition.json ABANDONED-DEAD-END). Shipping auto-listing intentionally skipped.` an |
| `get-centraldispatch-auth-url` | — | shippingService.ts:87 | Do NOT deploy or repoint — shipping is a DELETED feature per platform-hygiene.md. Minimal guard edit in nuke_frontend/src/services/shippingService.ts getCentralDispatchAuthUrl() (line ~86): replace the supabase.functions.invoke call with an immediate honest failure, e.g. `throw new Error('Central Dispatch shipping integration is retired and unavailable');` (AddVehicle feature-unavailable pattern — no network call, no |
| `place-bid-with-deposit` | — | auctionPaymentService.ts:108 | DO NOT redeploy the fn (ledger says so 4x) and DO NOT rip the bidding UI — feature removal is OWNER-ONLY per platform-hygiene.md + liveness-and-intent.md. Two honest-degrade options, both non-destructive, for owner to bless: (1) MINIMAL / already near-fine: the 404 already blocks fake success; the only gap is the misleading 'Failed to place bid' copy. In AuctionBiddingInterface.tsx handlePlaceBid, before the payments |
| `send-shipping-notification` | — | ShippingNotificationManager.tsx:188 | Do NOT deploy (source gone, fate=DO-NOT-REBUILD) and do NOT repoint (no canonical shipping fn exists — capability is retired). Do NOT delete the component (owner-only; it was already restored once after a wrong deletion). Apply an honest retired-feature guard at the single call site, sendCustomNotification() in ShippingNotificationManager.tsx:182-219: remove the supabase.functions.invoke('send-shipping-notification') |

## GUARD (gone, no replacement — honest degrade)

| fn | target | call sites | action |
|----|--------|-----------|--------|
| `analyze-and-source-pending` | — | AdminPendingVehicles.tsx:44; AdminPendingVehicles.tsx:65; AdminPendingVehicles.tsx:83 | Guard at the page level (AddVehicle pattern), one edit covering all three call sites in nuke_frontend/src/pages/AdminPendingVehicles.tsx: (1) add a module const FEATURE_AVAILABLE = false (or a small deployed-fn probe) and in loadAnalysis() short-circuit before invoke — setAnalysis(null), set a new `unavailable` state, and render a static notice: 'Pending-vehicle analysis is unavailable — its backend function (analyze |
| `analyze-batch-contextual` | — | ComprehensiveWorkOrderReceipt.tsx:608 | In ComprehensiveWorkOrderReceipt.tsx, apply the AddVehicle honest-degrade pattern inside runContextualBatchAnalysis (line ~555): add a top-of-function guard BEFORE the timeline_events 'processing' write — e.g. `const CONTEXTUAL_ANALYSIS_AVAILABLE = false; if (!CONTEXTUAL_ANALYSIS_AVAILABLE) { setContextualError('AI evidence analysis is temporarily unavailable.'); return; }` — so no DB status churn and no fake 'Analyz |
| `backfill-invoice-drafts` | — | InvoiceManager.tsx:262; ComprehensiveWorkOrderReceipt.tsx:637 | Guard both call sites; do not remove UI (owner-only). (1) InvoiceManager.tsx: in generateDraftInvoices, replace the invoke with an immediate honest degrade — `setDraftMessage('Draft-invoice generation is unavailable: the backfill-invoice-drafts service is not deployed.'); return;` before the try block (keep the dead invoke code commented or behind a `const DRAFT_BACKFILL_AVAILABLE = false` flag), and optionally disab |
| `backfill-profile-stats` | drain_vehicle_*_queue() SQL routines via per-minute crons (enqueue into vehicle_ | MarketDataTools.tsx:48 | In MarketDataTools.tsx, guard the 'backfill' tool honestly (AddVehicle pattern): (a) at line 43-61, replace runBackfill's invoke with an immediate setBackfillResult({error: 'Feature unavailable — backfill-profile-stats was retired. Derived stats now recompute automatically via the vehicle_*_recompute_queue crons (see docs/ledger/CAPABILITY_MAP.md, Derived-field recompute).'}) and never call supabase.functions.invoke; |
| `calculate-advanced-metrics` | — | marketIndexService.ts:458 | In nuke_frontend/src/services/marketIndexService.ts, replace the body of calculateAdvancedMetrics (lines 452-473) with an honest feature-unavailable degrade (AddVehicle pattern): remove the supabase.functions.invoke('calculate-advanced-metrics', ...) call and instead `throw new Error('Advanced metrics recalculation is not available — the calculate-advanced-metrics function was retired (Mar 2026). Historical metrics r |
| `calculate-market-indexes` | api-v1-market-trends (read-side only; no deployed recalc equivalent exists) | marketIndexService.ts:336 | Do NOT deploy (graveyard per CAPABILITY_MAP.md:55; SUPERSEDED per disposition.json). Edit marketIndexService.ts:334-348: replace the invoke with an honest feature-unavailable guard — e.g. `static async recalculateIndexes(_indexCode?: string): Promise<never> { throw new Error('Index recalculation is unavailable — the market-index engine was superseded (see docs/ledger/CAPABILITY_MAP.md, Market trends → api-v1-market-t |
| `discover-cl-squarebodies` | extract-craigslist (via import_queue) — extraction only; there is no deployed ca | CraigslistQueueDashboard.tsx:100; ScraperDashboard.tsx:70 | Do not deploy (graveyard per CAPABILITY_MAP.md:23) and do not remove UI (owner-only). Guard both call sites honestly: (1) CraigslistQueueDashboard.tsx — in triggerDiscovery() (line 98-115), replace the invoke with an immediate feature-unavailable path: setState/alert 'Craigslist discovery is retired — Craigslist listings now flow through extract-craigslist via import_queue' and disable the Trigger Discovery button (A |
| `encrypt-stripe-key` | — | StripeKeysManager.tsx:106 | StripeKeysManager.tsx already has the honest-degrade machinery (unavailable state + FEATURE_UNAVAILABLE_KEY + the 'not enabled on this deployment yet' card, lines 5-16/173-186), currently driven only by the user_stripe_keys table 404. Extend it to the fn call: (1) in handleSave's catch (line 132), detect the missing-function case (FunctionsHttpError with status 404, or err.context?.status === 404) → localStorage.setI |
| `extract-using-catalog` | — | OrganizationServiceMapper.tsx:58 | In OrganizationServiceMapper.tsx extractServices(): remove the supabase.functions.invoke('extract-using-catalog', ...) block (lines 56-79) and replace with an honest AddVehicle-pattern degrade — setError('Service extraction from websites is not currently available — the backing extraction function was retired and has no deployed replacement.') and return early (or render the disabled state before the button so the us |
| `get-twitch-auth-url` | — | VehicleStreamingCard.tsx:90 | AddVehicle-pattern honest degrade in VehicleStreamingCard.tsx, keeping the manual-URL path intact: (1) In handleConnectTwitch (line 85), remove the supabase.functions.invoke('get-twitch-auth-url', ...) block and replace the body with showToast('Twitch account linking is unavailable right now — paste your Twitch channel URL below instead', 'error'); or better, (2) replace the 'Connect Twitch' button at line ~291 with  |
| `index-service-manual` | scripts/ingest-service-manual.py + scripts/library_ingest_to_db.py (offline scri | referenceDocumentService.ts:169 | Fix the guard placement in referenceDocumentService.ts, keep the UI (liveness law — no deletions). (1) In triggerIndexing (lines 143-189): remove the blanket throw at line 149 and delete the two dead invoke branches ('index-reference-document' lines 152-166 and 'index-service-manual' lines 167-179, both graveyard per CAPABILITY_MAP:93 — never resurrect). Replace the body with a non-throwing honest degrade: console.in |
| `parse-dealer-spreadsheet` | — | DealerBulkEditor.tsx:277 | In DealerBulkEditor.tsx parseSpreadsheet(): replace the invoke block (lines ~276-296) with an honest degrade per the AddVehicle pattern — after the client-side ExcelJS parse, do NOT call the function; instead setAiParsing(false) and surface 'AI spreadsheet parsing is not available yet — add vehicles with the Add Row button instead' (alert or inline banner matching the page's existing alert() idiom). Better: gate earl |
| `phone-verify` | — | ProfileVerification.tsx:217; ProfileVerification.tsx:246; ProfileVerification.tsx:273 | GUARD all three handlers to degrade honestly (AddVehicle pattern); do NOT remove UI (deletion is owner-only per liveness law), keep the phone form and phoneVerified badge intact. (1) Add near top of component: `const PHONE_VERIFY_AVAILABLE = false; // phone-verify edge fn not deployed - see docs/ledger`. (2) At the entry of sendPhoneVerification (~line 214), sendPhoneVerificationVoice (~244), and verifyPhoneCode (~27 |
| `process-payout` | — | creditsService.ts:164 | Guard in creditsService.ts requestPayout() so it degrades honestly instead of manufacturing false success. Concretely: before/instead of inserting builder_payouts and invoking process-payout, return false (or throw a typed 'PayoutsNotAvailableError') with a clear log 'Builder payouts are not yet available (settlement path unbuilt — INTENT_LEDGER Gap #4)'. Do NOT insert a pending builder_payouts row that no processor  |
| `scrape-ksl-listings` | — | KSLScraper.tsx:53 | Guard the call site so it degrades honestly (AddVehicle pattern), do NOT delete the page (UI removal is owner-only, even though CAPABILITY_MAP.md:136 names admin/UnifiedScraperDashboard.tsx as the canonical successor). In KSLScraper.tsx handleScrape(), replace the supabase.functions.invoke('scrape-ksl-listings', ...) block (lines 51-79) with an immediate honest failure instead of a network call: setScraping(false); s |
| `send-title-capture-prompt` | — | TitleScan.tsx:106 | Guard, do not deploy or repoint, do not remove UI (owner-only). No deployable source exists to restore, no canonical fn serves 'send a remote title-capture link', and the /capture-title landing route the link depends on is also gone — so repointing to send-transaction-sms or user_notifications would still yield a dead link (fake success). Make handleSendPrompt (TitleScan.tsx:97-132) degrade honestly instead of invoki |
| `simple-scraper` | — | EditVehicle.tsx:277; VehicleComments.tsx:222; VehicleProfileContext.tsx:798; listingURLParser.ts:607 | Guard each site to degrade honestly; do NOT repoint to `ingest` (contract + intent mismatch — see risk). Concretely: (1) EditVehicle.tsx:277 — replace the simple-scraper invoke with an honest unavailable path: setScrapingError('URL import is temporarily unavailable') and keep the button disabled; never partially populate form fields. This is an edit-EXISTING-vehicle form, so routing to ingest (which match-or-creates) |
| `split-vehicle-from-source` | — | VehicleBasicInfo.tsx:345 | GUARD (do not deploy — no source; do not repoint — no canonical owner; do not rip UI — owner-only per liveness law). At VehicleBasicInfo.tsx:345, replace the invoke/navigate body of splitFromExternalListing with an honest early return: after the existing confirm(), call showToast('Splitting a source into a new vehicle isn't available yet — use Detach to break the listing linkage instead.', 'info', 5200) and return; d |

## ALREADY DEGRADES HONESTLY (optional polish only)

| fn | target | call sites | action |
|----|--------|-----------|--------|
| `auto-detect-vehicle-owner` | — | QuickRelationshipEditor.tsx:78 | No repoint target exists and the ledger forbids deploying (ABANDONED-DEAD-END). The call site already degrades honestly. Optional micro-cleanup only: in QuickRelationshipEditor.tsx, replace the detectOwner useEffect body (lines ~67-96) with an immediate `setDetectedOwner(null); setProfileMerges([]);` short-circuit — i.e. delete the supabase.functions.invoke('auto-detect-vehicle-owner', ...) call and its detectingOwne |
| `disconnect-facebook` | — | FacebookConnectionSettings.tsx:54 | No functional edit required — the call site already degrades honestly: disconnect works entirely through the external_identities row delete, with an explicit success alert and an error alert on failure; the edge-function invoke is dead commented code. Optional hygiene (safe, 3-line change): delete the commented block at FacebookConnectionSettings.tsx:53-56 so future audits stop flagging a phantom dependency; do NOT u |
| `live-admin` | — | liveService.ts:24; liveService.ts:50; liveService.ts:65; liveService.ts:74; liveService.ts:83; liveService.ts:92 | No edit required — the callsite layer already guards itself honestly via LIVE_ADMIN_ENABLED=false and degrades with a truthful 'not yet available' message (the AddVehicle pattern is effectively already implemented). Optional polish (single edit): liveService.ts:83 getSettings — return a shape like { unavailable: true } instead of {} so VehicleStreamingCard can render an explicit feature-unavailable state rather than  |
